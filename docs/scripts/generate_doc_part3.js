// Part 3: Chapters 4-5 — Clean academic formatting
const P1 = require('./generate_doc_part1');
const { chapterTitle, h2, h3, p, bp, bull, nItem, tc, code, TNR,
  Table, TableRow, TableCell, WidthType, ShadingType, Paragraph, TextRun, AlignmentType, PageBreak } = P1;

function getChapter4() {
  return [
    ...chapterTitle("4", "System Design"),

    h2("4.1 Introduction"),
    p("This chapter presents the system design of the AI-integrated decentralized e-voting system through various design diagrams. Data Flow Diagrams (DFDs) illustrate the flow of data through the system, while UML diagrams model the system's structure and behavior. These diagrams provide a comprehensive visual representation of the system architecture, component interactions, and process workflows."),

    h2("4.2 DFD Diagrams"),
    h3("4.2.1 Context Level DFD (Level 0)"),
    p("The context-level DFD shows the system as a single process interacting with three external entities: Voter, Admin, and Ethereum Blockchain. The Voter provides authentication credentials (wallet address, face image, OTP codes) and receives voting confirmation. The Admin provides election configuration (candidates, election keys, voter registration data) and receives election results. The Ethereum Blockchain stores and retrieves voter records, encrypted ballots, and candidate data."),

    h3("4.2.2 Level 1 DFD"),
    p("The Level 1 DFD decomposes the system into six major processes:"),
    nItem(1, "Voter Registration Process: Receives voter details and face image from Admin, stores face descriptor in SQLite database, and registers faceHash on the blockchain via registerVoter() smart contract function."),
    nItem(2, "Authentication Process: Handles three-factor verification — wallet check against blockchain, OTP generation and verification via Email/SMS, and face recognition comparison using Euclidean distance."),
    nItem(3, "Vote Encryption Process: Retrieves election public key from blockchain, performs client-side RSA-2048 encryption of candidate ID, and submits encrypted ballot to the vote() smart contract function."),
    nItem(4, "Anti-Coercion Monitoring Process: Continuously analyzes webcam frames for multiple face detection, identity verification against stored descriptor, and triggers lockout on violations."),
    nItem(5, "Booth Mode Process: Manages supervised voting with voter verification via face scan, booth vote submission via boothVote() function, and VVPAT receipt generation."),
    nItem(6, "Result Tallying Process: Collects SSS shares, reconstructs private key, reads encrypted ballots from blockchain, decrypts votes, and computes candidate-wise results."),

    h3("4.2.3 Level 2 DFD — Authentication Sub-process"),
    p("The authentication process is further decomposed into three sub-processes: (a) Wallet Verification — queries the backend to check if the wallet address is registered; (b) Dual-Channel OTP — generates 6-digit codes, sends via Nodemailer (Email) and Twilio (SMS), verifies both codes; (c) Face Recognition — captures live image, extracts 128-D descriptor via SSD MobileNet v1, compares with stored descriptor using Euclidean distance threshold of 0.65."),

    h2("4.3 UML Diagrams"),
    h3("4.3.1 Use Case Diagram"),
    p("The Use Case diagram identifies two primary actors — Voter and Admin — and their interactions with the system:"),
    bp("Voter Use Cases: ", "Connect Wallet, Verify Face, Submit OTP, Cast Encrypted Vote, View Vote Confirmation, Request Gas Funds, Update Contact Information."),
    bp("Admin Use Cases: ", "Generate Election Keys, Register Voter (Face Capture), Add Candidate, Set Election Dates, View/Reset Candidates, Reset Voters, Distribute Key Shares, Enter Tally Shares, View Election Results, Operate Booth Mode, Transfer Contract Ownership, Fund Voter Wallets."),

    h3("4.3.2 Sequence Diagram"),
    p("The Sequence diagram illustrates the temporal flow of the voting process:"),
    nItem(1, "Voter → MetaMask: Request wallet connection"),
    nItem(2, "MetaMask → Frontend: Return wallet address"),
    nItem(3, "Frontend → Backend: GET /voter/{address} (registration check)"),
    nItem(4, "Frontend → Face-api.js: Detect face and extract descriptor"),
    nItem(5, "Face-api.js → Frontend: Return 128-D descriptor with Euclidean distance"),
    nItem(6, "Frontend → OTP Service: POST /send-otp (Email) and POST /send-sms (SMS)"),
    nItem(7, "Voter → Frontend: Enter SMS OTP and Email OTP"),
    nItem(8, "Frontend → OTP Service: POST /verify-dual-otp"),
    nItem(9, "Frontend → Smart Contract: Read electionPublicKey"),
    nItem(10, "Frontend → JSEncrypt: RSA-2048 encrypt(candidateId, publicKey)"),
    nItem(11, "Frontend → Smart Contract: vote(encryptedBallot) via MetaMask transaction"),
    nItem(12, "Smart Contract → Blockchain: Store encrypted vote, set hasVoted = true"),

    h3("4.3.3 Class Diagram"),
    p("The Class diagram models the key entities in the system:"),
    bp("Voter: ", "Attributes — isRegistered (bool), hasVoted (bool), encryptedVote (string), faceHash (bytes32). Relationships — Associated with one Wallet Address, submits one EncryptedBallot."),
    bp("Candidate: ", "Attributes — id (uint256), name (string), voteCount (uint256). Relationships — Receives multiple EncryptedBallots after decryption."),
    bp("VotingContract: ", "Attributes — owner (address), electionPublicKey (string), isElectionOpen (bool), voters (mapping), candidates (array). Methods — registerVoter(), vote(), boothVote(), addCandidate(), getCandidates(), setElectionPublicKey()."),
    bp("OTPService: ", "Attributes — otpStorage (map). Methods — generateOTP(), sendEmail(), sendSMS(), verifyDualOTP()."),
    bp("BackendServer: ", "Attributes — database (SQLite). Methods — registerVoter(), getVoter(), addCandidate(), getCandidates(), updateVoter(), faucet()."),

    h3("4.3.4 Collaboration Diagram"),
    p("The Collaboration diagram shows the structural organization of objects that interact during the vote casting process. The Frontend component collaborates with MetaMask for wallet interaction, Face-api.js for biometric processing, JSEncrypt for ballot encryption, and the Smart Contract for transaction submission. The Backend collaborates with SQLite for data persistence and the OTP Service for verification code generation and validation."),

    h3("4.3.5 Activity Diagram"),
    p("The Activity diagram models the complete voting workflow:"),
    p("Start → Connect MetaMask Wallet → [Decision: Registered?] → Yes: Proceed / No: Show Error → Capture Face Image → Extract 128-D Descriptor → [Decision: Distance < 0.65?] → Yes: Face Match / No: Reject → Send Dual OTP → [Decision: OTP Valid?] → Yes: Authenticated / No: Retry → Display Candidates → Select Candidate → Encrypt Vote (RSA-2048) → Submit to Blockchain → [Decision: Transaction Confirmed?] → Yes: Show Receipt / No: Show Error → End."),
    p("Parallel Activity: Anti-Coercion Monitor runs continuously during the voting phase, checking for multiple faces every 1 second. If a violation is detected for more than 5 seconds, a booth lockout is triggered."),
  ];
}

function getChapter5() {
  return [
    ...chapterTitle("5", "System Coding & Testing"),

    h2("5.1 Introduction"),
    p("This chapter describes the implementation details of the AI-integrated decentralized e-voting system. It covers the technologies and frameworks used, provides sample code from key modules, and discusses the software testing techniques and strategies employed to ensure system reliability and security."),

    h2("5.2 Implementation"),
    p("The system was implemented as a multi-service architecture with the following components running concurrently:"),
    nItem(1, "Hardhat Ethereum Node: Local blockchain running on port 8545, providing 20 pre-funded test accounts. For production, Sepolia testnet via Alchemy RPC is used."),
    nItem(2, "Backend Node.js Server (Port 5000): Express.js server managing voter CRUD operations, candidate management, election configuration, and wallet funding via Ethers.js."),
    nItem(3, "OTP Service (Port 4000): Express.js server handling dual-channel OTP generation and verification using Nodemailer (Gmail SMTP) and Twilio SDK."),
    nItem(4, "Python FastAPI Server (Port 8000): Handles face verification using OpenCV Haar cascades and histogram comparison, plus anti-coercion monitoring endpoints."),
    nItem(5, "Next.js Frontend (Port 3000): React-based DApp with face-api.js for client-side CNN processing, JSEncrypt for RSA-2048 encryption, and ethers.js for blockchain interactions via MetaMask."),

    h2("5.3 About Technologies Used"),
    h3("5.3.1 Ethereum & Solidity"),
    p("Ethereum is an open-source, distributed computing platform featuring smart contract functionality. Solidity (v0.8.28) is an object-oriented programming language used for writing smart contracts on the Ethereum blockchain. The Voting.sol contract handles all on-chain logic including voter registration, encrypted ballot storage, and candidate management."),
    h3("5.3.2 React.js & Next.js"),
    p("React.js is a JavaScript library for building user interfaces. Next.js is a React framework that provides server-side rendering, file-based routing, and optimized production builds. The frontend uses React hooks for state management and Next.js pages for routing."),
    h3("5.3.3 Node.js & Express.js"),
    p("Node.js is a JavaScript runtime built on Chrome's V8 engine. Express.js is a minimal web framework for Node.js. The backend servers use Express for RESTful API endpoints, middleware for CORS handling, and SQLite3 for database operations."),
    h3("5.3.4 Python & FastAPI"),
    p("Python is a high-level programming language used for the AI processing backend. FastAPI is a modern Python web framework for building APIs. OpenCV (cv2) handles image processing, numpy handles numerical computations, and the cryptography library handles data encryption."),
    h3("5.3.5 Face-api.js & TensorFlow.js"),
    p("Face-api.js is a JavaScript face detection and recognition API built on top of TensorFlow.js. It provides SSD MobileNet v1 for face detection, face landmark detection for 68 facial points, and face recognition for 128-dimensional descriptor extraction. All processing runs client-side in the browser."),
    h3("5.3.6 Hardhat"),
    p("Hardhat is an Ethereum development environment for compiling, deploying, testing, and debugging smart contracts. It provides a local Ethereum network, Solidity compiler integration, and testing utilities with Mocha and Chai."),
    h3("5.3.7 MetaMask"),
    p("MetaMask is a cryptocurrency wallet and browser extension that enables users to interact with Ethereum DApps. It manages private keys, signs transactions, and provides the connection between the browser and the Ethereum network."),

    h2("5.4 Sample Code"),
    h3("5.4.1 Smart Contract — Voting.sol"),
    code("// SPDX-License-Identifier: MIT"),
    code("pragma solidity ^0.8.28;"),
    code(""),
    code("contract Voting {"),
    code("    struct Candidate {"),
    code("        uint256 id;"),
    code("        string name;"),
    code("        uint256 voteCount;"),
    code("    }"),
    code("    struct Voter {"),
    code("        bool isRegistered;"),
    code("        bool hasVoted;"),
    code("        string encryptedVote;"),
    code("        bytes32 faceHash;"),
    code("    }"),
    code("    address public owner;"),
    code("    string public electionPublicKey;"),
    code("    mapping(address => Voter) public voters;"),
    code("    Candidate[] public candidates;"),
    code(""),
    code("    modifier onlyOwner() {"),
    code('        require(msg.sender == owner, "Only owner");'),
    code("        _;"),
    code("    }"),
    code("    constructor() { owner = msg.sender; }"),
    code(""),
    code("    function registerVoter(address _v, bytes32 _fh)"),
    code("        external onlyOwner {"),
    code("        require(!voters[_v].isRegistered,"),
    code('            "Already registered");'),
    code("        voters[_v].isRegistered = true;"),
    code("        voters[_v].faceHash = _fh;"),
    code("    }"),
    code("    function vote(string memory _enc) external {"),
    code("        require(voters[msg.sender].isRegistered);"),
    code("        require(!voters[msg.sender].hasVoted);"),
    code("        voters[msg.sender].hasVoted = true;"),
    code("        voters[msg.sender].encryptedVote = _enc;"),
    code("    }"),
    code("}"),

    h3("5.4.2 Vote Casting with RSA Encryption (Frontend)"),
    code("const castVote = async (candidateId) => {"),
    code("    const publicKey = await contract.electionPublicKey();"),
    code("    const encryptor = new JSEncrypt();"),
    code("    encryptor.setPublicKey(publicKey);"),
    code("    const encryptedVote = encryptor"),
    code("        .encrypt(candidateId.toString());"),
    code("    const tx = await contract.vote(encryptedVote);"),
    code("    await tx.wait();"),
    code('    localStorage.setItem("hasVoted", "true");'),
    code("};"),

    h3("5.4.3 Shamir's Secret Sharing — Key Distribution"),
    code("const secrets = require('secrets.js-grempe');"),
    code("const hexKey = secrets.str2hex(privateKey);"),
    code("// Split into 5 shares, threshold 3"),
    code("const shares = secrets.share(hexKey, 5, 3);"),
    code("// Reconstruction (during tally)"),
    code("const combined = secrets.combine(shares.slice(0,3));"),
    code("const recovered = secrets.hex2str(combined);"),

    h3("5.4.4 Face Recognition — Euclidean Distance"),
    code("const detection = await faceapi"),
    code("    .detectSingleFace(img)"),
    code("    .withFaceLandmarks()"),
    code("    .withFaceDescriptor();"),
    code("const stored = new Float32Array("),
    code("    JSON.parse(voterData.face_descriptor));"),
    code("const distance = faceapi.euclideanDistance("),
    code("    detection.descriptor, stored);"),
    code("if (distance < 0.65) { /* Face verified */ }"),

    h3("5.4.5 OTP Service — Dual Channel Verification"),
    code("// Email OTP via Nodemailer"),
    code("const transporter = nodemailer.createTransport({"),
    code("    service: 'gmail',"),
    code("    auth: { user: EMAIL_USER, pass: EMAIL_PASS }"),
    code("});"),
    code("await transporter.sendMail({"),
    code("    to: email,"),
    code("    subject: 'Voting Verification Code',"),
    code("    text: `Your OTP is: ${otp}`"),
    code("});"),
    code("// SMS OTP via Twilio"),
    code("const client = twilio(SID, TOKEN);"),
    code("await client.messages.create({"),
    code("    body: `Your OTP is: ${otp}`,"),
    code("    from: TWILIO_PHONE, to: mobile"),
    code("});"),

    h3("5.4.6 Anti-Coercion Monitoring"),
    code("// Continuous monitoring every 1 second"),
    code("const detections = await faceapi"),
    code("    .detectAllFaces(video)"),
    code("    .withFaceLandmarks()"),
    code("    .withFaceDescriptors();"),
    code("if (detections.length > 1) {"),
    code("    violation = 'multiple'; // Coercion"),
    code("} else if (detections.length === 1) {"),
    code("    const dist = faceapi.euclideanDistance("),
    code("        detections[0].descriptor,"),
    code("        verifiedDescriptor);"),
    code("    if (dist > 0.45) violation = 'unrecognized';"),
    code("}"),
    code("// 5-second grace, then tiered lockout"),

    h2("5.5 Software Testing Techniques"),
    h3("5.5.1 Unit Testing"),
    p("Individual modules were tested in isolation. Smart contract functions (registerVoter, vote, boothVote, addCandidate) were tested using Hardhat's testing framework with Mocha and Chai. Backend API endpoints were tested using Python's unittest framework and Node.js test scripts. Face recognition accuracy was validated against the LFW benchmark dataset."),
    h3("5.5.2 Integration Testing"),
    p("Integration testing verified the interaction between modules. End-to-end flows were tested including: wallet connection → face verification → OTP verification → vote casting → blockchain confirmation. Cross-service communication between the frontend, backend, OTP service, and smart contract was verified."),
    h3("5.5.3 Functional Testing"),
    p("Each feature was tested against specified requirements: voter registration with face capture, three-factor authentication pipeline, encrypted vote casting and blockchain recording, anti-coercion detection with multiple-face scenarios, booth mode voting with VVPAT, and result tallying with SSS key reconstruction."),

    h2("5.6 Software Testing Strategies"),
    h3("5.6.1 Black Box Testing"),
    p("The system was tested from an external perspective without knowledge of internal implementation. Test cases included: valid voter registration, duplicate voter detection, invalid OTP rejection, face mismatch rejection, double voting prevention, and unauthorized admin access prevention."),
    h3("5.6.2 White Box Testing"),
    p("Internal code paths were tested thoroughly. Smart contract edge cases including gas estimation, revert conditions, and event emissions were verified. Frontend state management flows, error handling paths, and edge cases in face recognition (no face, multiple faces, low confidence) were tested."),
    h3("5.6.3 Security Testing"),
    p("The system was tested against six attack categories: Replay attacks (OTP expiry after 300 seconds), Sybil attacks (one-vote-per-wallet enforcement), Man-in-the-Middle attacks (client-side RSA encryption), Coercion attacks (AI monitoring with lockout), Spoofing attacks (0.45 Euclidean distance threshold), and Key compromise attacks (SSS 3-of-5 threshold requirement)."),
    h3("5.6.4 Performance Testing"),
    p("System performance was evaluated under various load conditions. Transaction latency was measured for 100 concurrent vote submissions (average 4.8 seconds). Client-side RSA-2048 encryption time was benchmarked (average 42ms). Gas consumption was measured at approximately 45,000 gas units per vote transaction."),
  ];
}

module.exports = { getChapter4, getChapter5 };
