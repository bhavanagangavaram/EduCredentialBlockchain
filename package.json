{
  "dependencies": {
    "axios": "^1.13.4",
    "concurrently": "^9.1.2",
    "ethers": "^6.16.0",
    "node-jsencrypt": "^1.0.0",
    "playwright": "^1.59.1",
    "sqlite3": "^5.1.7"
  },
  "scripts": {
    "start": "concurrently \"cd contracts && npx hardhat node\" \"cd backend-node && npm start\" \"cd otp-service && npm start\" \"cd frontend && npm run dev\"",
    "deploy": "cd contracts && npx hardhat run scripts/deploy.js --network localhost",
    "setup": "npm install && cd contracts && npm install && cd ../backend-node && npm install && cd ../otp-service && npm install && cd ../frontend && npm install"
  },
  "devDependencies": {
    "docx": "^9.6.1"
  }
}
